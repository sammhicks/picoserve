[package]
name = "state_local"
version = "0.1.0"
edition = "2024"
description = "Stateful Applications with data coming from the connection."
categories = ["Tokio"]
publish = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = { workspace = true }
log = { workspace = true }
pretty_env_logger = { workspace = true }
picoserve = { path = "../../picoserve", features = ["log", "tokio", "json"] }
serde = { workspace = true }
tokio = { workspace = true }
