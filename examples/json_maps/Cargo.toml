[package]
name = "json_maps"
version = "0.1.0"
edition = "2021"
description = "An example showing different JSON values that maps can be serialized to"
categories = ["Tokio", ":pinned"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = { workspace = true }
log = { workspace = true }
picoserve = { path = "../../picoserve", features = ["json", "log", "tokio"] }
pretty_env_logger = { workspace = true }
tokio = { workspace = true }
serde = { workspace = true }
