[package]
name = "nested_router"
version = "0.1.0"
edition = "2021"
description = "Nesting `Router`s inside other `Router`s."
categories = ["Tokio"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = { workspace = true }
log = { workspace = true }
pretty_env_logger = { workspace = true }
picoserve = { path = "../../picoserve", features = ["log", "tokio", "json"] }
tokio = { workspace = true }
